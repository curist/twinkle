// Passing xs to a user function is treated as retain-capable.
// Must not rewrite subsequent update.

fn touch(v: Vector<Int>) Int {
    v.len()
}

fn make() Vector<Int> {
    xs := [1, 2, 3]
    n := touch(xs)
    println("${n}")
    xs[0] = 99
    xs
}

println("${make()[0]}")
