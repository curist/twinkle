// Reading accumulator inside loop body blocks builder rewrite.
// Expected output:
//   0
//   1
//   2
//   3

fn make() Vector<Int> {
    xs: Vector<Int> = []
    for x in [1, 2, 3] {
        println("${xs.len()}")
        xs = xs.push(x)
    }
    xs
}

println("${make().len()}")
