// Self-concat in a loop must not rewrite.
// Expected output:
//   4
//   1
//   2
//   1
//   2

fn dup() Vector<Int> {
    xs := [1, 2]
    for i in range(1) {
        _ := i
        xs = xs.concat(xs)
    }
    xs
}

out := dup()
println("${out.len()}")
for x in out {
  println("${x}")
}
