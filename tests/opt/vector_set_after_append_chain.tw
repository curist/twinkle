// Uniqueness should survive repeated push+reassign before a point update.
// Final set should rewrite.

fn make() Vector<Int> {
    xs := [1, 2, 3]
    xs = xs.append(4)
    xs = xs.append(5)
    xs[0] = 99
    xs
}

println("${make()[0]}")
