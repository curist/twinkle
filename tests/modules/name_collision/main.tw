use util_a
use util_b

fn main() {
    // Both modules have a function named 'process' — must use qualified calls
    a := util_a.process(5)
    b := util_b.process(5)
    println("${a}")
    println("${b}")
}
