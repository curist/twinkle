// Expected output:
//   first=0

it := Iterator.unfold(0, fn(i: Int) {
  if i < 3 { UnfoldStep.Yield(i * 10, i + 1) }
  else     { UnfoldStep.Done }
})

case Iterator.next(it) {
  .Some(item) => println("first=${item.value}"),
  .None => println("first=none"),
}
