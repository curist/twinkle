// Module B: uses module A, exports functions that work with A's types
use .point.{Point}

pub fn origin() Point {
  Point.{ x: 0, y: 0 }
}

pub fn move_right(p: Point, n: Int) Point {
  p.translate(n, 0)
}

pub fn describe(p: Point) String {
  "(${p.x}, ${p.y})"
}
