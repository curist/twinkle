use tests.cell_suite
use tests.column_suite
use tests.csv_suite
use tests.table_suite
use tests.row_suite
use tests.query_suite
use runner

runner.run_all([
  cell_suite.suite(),
  column_suite.suite(),
  csv_suite.suite(),
  table_suite.suite(),
  row_suite.suite(),
  query_suite.suite(),
])
