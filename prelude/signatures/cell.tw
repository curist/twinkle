/// Create a mutable cell containing value.
pub fn new<T>(value: T) Cell<T> {
  value
}

/// Read the current value from a cell.
pub fn get<T>(cell: Cell<T>) T {
  0
}

/// Overwrite the current value in a cell.
pub fn set<T>(cell: Cell<T>, value: T) Void {
}

/// Apply f to the current value and store the result.
pub fn update<T>(cell: Cell<T>, f: fn(T) T) Void {
}
