use @std.testing as runner
use .suites.backend_verify_suite
use .suites.codegen_integration_suite
use .suites.insert_boundaries_suite
use .suites.linker_suite
use .suites.runtime_suite
use .suites.slot_assign_suite
use .suites.wasm_ir_suite
use .suites.wasm_layout_suite
use .suites.wasm_plan_suite
use .suites.wat_suite

runner.run_all([
  wasm_ir_suite.suite(),
  wasm_layout_suite.suite(),
  wasm_plan_suite.suite(),
  insert_boundaries_suite.suite(),
  slot_assign_suite.suite(),
  wat_suite.suite(),
  linker_suite.suite(),
  runtime_suite.suite(),
  codegen_integration_suite.suite(),
  backend_verify_suite.suite(),
])
