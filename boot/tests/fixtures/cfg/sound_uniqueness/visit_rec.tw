pub fn visit(seen: Vector<Int>, n: Int) Vector<Int> {
  seen[0] = n
  if n <= 0 {
    seen
  } else {
    visit(seen, n - 1)
  }
}

pub fn go() Int {
  m: Vector<Int> = collect _ in range(1) {
    0
  }
  visit(m, 3).at(0)
}
