pub type State = .{ xs: Vector<Int>, spare: Vector<Int> }

pub fn go() Int {
  xs: Vector<Int> = collect _ in range(1) {
    0
  }
  spare: Vector<Int> = collect _ in range(1) {
    99
  }
  st := State.{ xs, spare }
  st.xs[0] = 7
  st.xs.at(0)
}

println(go().to_string())
