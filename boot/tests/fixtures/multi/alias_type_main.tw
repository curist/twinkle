use .alias_type_bridge
use .alias_type_source.{Diagnostic}

fn read(d: Diagnostic) String {
  d.message
}

fn main() {
  single := alias_type_bridge.make("boom")
  many := alias_type_bridge.batch("ok")

  read(single)
  many.len()
}
