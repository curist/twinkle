fn build() Dict<String, Int> {
  d: Dict<String, Int> = Dict.new()
  d = .set("a", 1)
  d = .set("b", 2)
  d = .set("c", 3)
  d = .set("d", 4)
  d = .remove("b")
  d = .remove("z")
  d
}

fn main() {
  d := build()
  line := ""
  for k, v in d {
    line = .concat("${k}=${v};")
  }
  println(line)
  println(d.len().to_string())
  println(d.has("b").to_string())
}

main()
