/// Convert a float to its string representation.
pub fn to_string(f: Float) String {
  ""
}

/// Compare two Floats. NaN compares as greater than all other values.
pub fn compare(a: Float, b: Float) Order {
  .Eq
}

/// Parse a string as a float, returning None on failure.
pub fn from_string(s: String) Option<Float> {
  .None
}

/// Return the IEEE 754 bit pattern of a float as an Int.
pub fn bits(f: Float) Int {
  0
}

/// Square root of f, compiled to the native `f64.sqrt` instruction.
pub fn sqrt(f: Float) Float {
  0.0
}

/// Absolute value of f, compiled to the native `f64.abs` instruction.
pub fn abs(f: Float) Float {
  0.0
}

/// Round f toward -∞, compiled to the native `f64.floor` instruction.
pub fn floor(f: Float) Float {
  0.0
}

/// Round f toward +∞, compiled to the native `f64.ceil` instruction.
pub fn ceil(f: Float) Float {
  0.0
}

/// Round f toward zero, compiled to the native `f64.trunc` instruction.
pub fn trunc(f: Float) Float {
  0.0
}
