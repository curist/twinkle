/// Return a new dict with key set to value.
pub fn set<K, V>(d: Dict<K, V>, k: K, v: V) Dict<K, V> {
  d
}

/// Return all keys from the dict.
pub fn keys<K, V>(d: Dict<K, V>) Vector<K> {
  []
}

/// Create an empty dict.
pub fn new<K, V>() Dict<K, V> {
  []
}

/// Return the number of entries in the dict.
pub fn len<K, V>(d: Dict<K, V>) Int {
  0
}

/// Return true when the dict has no entries.
pub fn is_empty<K, V>(d: Dict<K, V>) Bool {
  false
}

/// Return true when key exists in the dict.
pub fn has<K, V>(d: Dict<K, V>, k: K) Bool {
  false
}

/// Return the value for key, or None if absent.
pub fn get<K, V>(d: Dict<K, V>, k: K) Option<V> {
  .None
}

/// Return a new dict with key removed.
pub fn remove<K, V>(d: Dict<K, V>, k: K) Dict<K, V> {
  d
}

/// Spike-only: select the next experimental builder execution order.
pub fn bench_builder_rotation(rotation: Int) Void {}

/// Spike-only: build one dense entry stream with each experimental HAMT builder.
pub fn bench_builders<K, V>(d: Dict<K, V>) Vector<Dict<K, V>> {
  []
}

/// Spike-only: return the latest dense-prep, builder, and order phase timings.
pub fn bench_timings() Vector<Float> {
  []
}

/// Spike-only: compare mutable and canonical stable-ID arena layouts.
pub fn bench_arena_layouts(n: Int, k: Int, churn_stride: Int) Vector<Dict<Int, Int>> {
  []
}

/// Spike-only: return clone, update, churn, seam, and freeze phase timings.
pub fn bench_arena_timings() Vector<Float> {
  []
}

/// Test-only: build a Dict from engineered (hash, key, value) triples, bypassing hash_key.
pub fn build_dense_test(hashes: Vector<Int>, keys: Vector<Int>, vals: Vector<Int>) Dict<Int, Int> {
  Dict.new()
}

/// Test-only: look up a key using an explicitly supplied hash (for engineered shapes).
pub fn get_by_hash(d: Dict<Int, Int>, hash: Int, key: Int) Option<Int> {
  .None
}
