use arg

pub type Command = .{
  name: String,
  help: String,
  args: Vector<arg.Arg>,
}

pub fn add_arg(cmd: Command, a: arg.Arg) Command {
  .{ name: cmd.name, help: cmd.help, args: cmd.args.push(a) }
}
